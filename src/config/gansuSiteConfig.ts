/**
 * gansuSiteConfig — static SiteSceneConfig + ASSET_REGISTRY for the Gansu site.
 * Contract: contracts/frontend/app_integration.md §5
 *
 * STUB — implementation pending gate approval (PR #45).
 * Exports the correct shape so module resolution succeeds in tests.
 */
import type { SiteSceneConfig, AssetRegistry } from "../scene/types";

export const GANSU_SITE_CONFIG = {} as SiteSceneConfig;
export const ASSET_REGISTRY = {} as AssetRegistry;
