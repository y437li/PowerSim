# LINEAGE — Decisions, Locked Contracts, Open Blockers

This file holds ONLY what git and GitHub cannot tell you. For everything else, read the source of record at the start of every session:

- `git log --oneline -15` — what has merged recently
- `gh pr list --state open` — what's in flight and its gate status (draft = contract+tests stage; ready = implementation stage; reviews and QA verdicts are on the PR)
- This file — binding decisions, locked shared contracts, open blockers

Do **not** log work milestones here — PR state is the status record. Append-only, three entry kinds:

- **DECISION** — binding choice by rl-architect: one line + the PR/commit where it was made. Supersede with a new entry referencing the old ID; never edit.
- **LOCKED** — a shared contract is locked: path + PR ref. Consumers may not deviate without a superseding DECISION.
- **BLOCKED** — work stuck on input: who/what is needed. Remove when resolved, citing the resolving PR.

## Decisions

- [D1] 2026-06-09 — Team, workflow (contract-first-dev / qa-verification), naming conventions, and PR-gated process established; see CLAUDE.md. (setup commits 3e9fdd8…)
- [D2] 2026-06-09 — Composable asset library added to the spec (REBUILD_SPEC.md §8): gas combustion, PEM + alkaline electrolyzers, 6 load archetypes; obs/action derived from site YAML; Gansu config is the parity special case. Build order: §3 plant first, §8 after baseline parity.
- [D3] 2026-06-10 — Δt = 1 hour (binding). Episodes 168 steps (7-day train) / 8760 steps (365-day eval); forecast = 24 steps at 1-step stride = next 24 h; tariff boundaries resolved at minute resolution (D8). Resolves the §6 15-min/1-h split and the ×4 stride bug at the source; §8 gas/electrolyzer asset distinctions survive at ≥15 min so 1 h loses no modeled physics. (PR #2)
- [D4] 2026-06-10 — SOC bounds = [0.2, 0.9] (binding). Hard-clip P_ch/P_dis so SOC lands exactly on the bound; overshoot → soc_violation_mwh, penalized 20 000 ¥/MWh (§3.6 row 5). Adopts the code-faithful value the §3 numbers and 294.5 MWh sizing are written against; "10–90%" was a stale doc note. (PR #2)
- [D5] 2026-06-10 — PCC export limit = single configurable site field grid_connection.max_export_mw; Gansu parity default 945 MW (the GridParams physics value §3.6 row 8 scaling/curtailment math was authored against). The YAML 200 MW figure is reclassified as an optional interconnection/contractual cap, out of scope v1 — NOT the physics export limit. (PR #2)
- [D6] 2026-06-10 — Forecast noise = horizon-scaled multiplicative Gaussian, applied in _get_obs. For horizon h=1..24 steps: x̂_h = x_true_h·(1+ε_h), ε_h~N(0, σ_h), σ_h = σ_max·(h/H_max), σ_max=0.10, H_max=24; clip each noised feature to its physical range; keys threaded explicitly per env (§7). Implements §2.1 "grows linearly with horizon, 10% at max" and fixes §6 "noise never applied". (PR #2)
- [D7] 2026-06-10 — Spread clamp ≥ 0. price_sell = max(0, price_buy − max(0, spread + N(0, σ_spread))), spread=30, σ_spread=10 ¥/MWh; clamp on both the spread and the final sell price. Closes the §6 risk-free-arbitrage hole while preserving the ¥30 mean discount. (PR #2)
- [D8] 2026-06-10 — Forecast/live price lookup is minute-aware (_get_price(hour, minute)). The 10:30 and 11:30 mid-tier boundaries are correct in both realized and forecast prices; at Δt=1 h steps land on :00 but the function stays correct under a future 15-min Δt. Fixes the §6 hour-only lookup bug. (PR #2)
- [D9] 2026-06-10 — Forecast stride = 1 step (no ×4), no episode-end wraparound. Samples t+1…t+24 steps; near episode end the window is clamped/truncated to the synthetic-year device array (lax.dynamic_slice), never % len(episode_data) — no cross-season leakage. Fixes both §6 stride bugs. (PR #2)
- [D10] 2026-06-10 — Demand charge booked once per calendar month; no terminal double-count. month_peak·demand_rate charged at each month boundary plus a single terminal flush for the final partial month; the terminal step does not re-book an already-charged month. cost_cum.c_demand_charge_yuan_cum reflects each month exactly once. Fixes the §6 terminal double-count. (PR #2)
- [D12] 2026-06-10 — Grid import limit is a per-site field grid_connection.max_import_mw, mirroring the export field (D5); Gansu parity default 400 MW (the §3.6 row 9 GridParams value). Resolves the backend N2 finding (import was implied-constant on the wire while export was per-site); telemetry pcc.max_import_mw carries it so the 3D can scale the import wire. (telemetry lock PR)
- [D13] 2026-06-10 — Cost telemetry separates REAL-MONEY from REWARD-BASIS as two independently-reconstructable totals. Real money: cost_total_real_yuan = c_energy + c_demand_charge + c_degradation + c_curtail + c_voll (the monthly demand charge month_peak·rate per D10; excludes the reward-shaping term and SOC penalty). Reward basis: cost_total_reward_basis_yuan = c_energy + 2.0·c_demand_shape + c_degradation + c_curtail + c_voll, with reward = −(cost_total_reward_basis_yuan + penalty_yuan)·1e-5 (§3.5). c_demand_shape carries the RAW C_DC_shape (the 2.0 weight is applied by the reward, not stored); c_import/r_export are a display-only decomposition of c_energy, not summands. eval_compare is real-money basis with SOC/penalty reported but excluded from total_cost_yuan. Resolves both reviewers' cost-accounting blockers on PR #2. (telemetry lock PR)
- [D11] 2026-06-10 — B2 parity strategy: build a from-scratch plain-Python/NumPy reference implementation of §3 physics + §4 generators, not vendored legacy (per direct user direction: legacy code is unavailable and is to be recreated from scratch). Lives under reference/ (NOT legacy/), built independently of the JAX core so JAX-vs-reference parity is two independent implementations of the same spec; §6 fixes D6–D10 are applied in BOTH. Gansu config (§8.4, D2) is the parity special case. Owner jax-env-engineer; contract contracts/env/reference_implementation.md, tests tests/env/test_env_reference_implementation.py + tests/env/test_env_parity_gansu.py. Recorded limitation: this validates spec-conformance and cross-implementation agreement, not behavioral equivalence with the original (unavailable) system. (PR #2)
- [D14] 2026-06-10 — Machine-checkable verdict-marker convention (the GitHub account is shared across agents, so `gh pr review --approve`/`--request-changes` 422s on a self-authored PR). Every review/QA verdict is a TOP-LEVEL PR comment: first line exactly `VERDICT: APPROVE|REQUEST_CHANGES|COMMENT|QA_PASS|QA_FAIL|QA_PASS_WITH_ISSUES`, second line `reviewer: <agent-name>` (+ optional scope). Newest marker per reviewer wins. Merge gate (`scripts/check_pr_gate.sh <pr> --required <reviewer>`): required reviewer's latest = APPROVE, QA's latest = QA_PASS (or QA_PASS_WITH_ISSUES + an rl-architect APPROVE marker), no reviewer's latest = REQUEST_CHANGES. CLAUDE.md amended accordingly. (process-conventions PR)
- [D15] 2026-06-10 — Tests-tree clarification (team-lead ruling): there is NO `tests/contracts/` directory — PR #4 scaffolding correctly omitted it. Tests for the shared/telemetry contract live under `tests/shared/`, an area that comes into existence by a future DECISION when its first feature (e.g. the telemetry schema validator, task #20) lands its contract+tests. Until then `tests/shared/` does not exist. (process-conventions PR)
- [D16] 2026-06-10 — `STACK.md` (root) is the binding per-area stack registry (USER-requested), derived from REBUILD_SPEC §5/§7, charters, and D1–D13. Maintenance rule added to CLAUDE.md engineering rules: any PR introducing or changing a stack element MUST update STACK.md in that PR, and reviewers check contracts/implementations against it. Version pins stay in pyproject/package.json; STACK.md pins a version only where the choice depends on it. (process-conventions PR)
- [D17] 2026-06-10 — Env-logic enhancements (REBUILD_SPEC §10): user approved **Tier 1 only — E2 (SOC/temperature-dependent efficiency) + E5 (forecast-error regime switching)** for build. E1 (battery aging) and E4 (weather/load coupling) are DEFERRED candidates; E3 (ramp limits) and E6 (dynamic grid) are PARKED. Each approved item ships as its own DECISION + contract + tests + parity regression, sequenced AFTER the §3 reference baseline (D11); all are toggles default-OFF so Gansu parity is never destabilized. Voltage/reactive/frequency remain out of scope regardless. (PR #9)

- [D18] 2026-06-10 — Machine-readable telemetry schema (additive to the LOCKED v1.0.0 contract, NO field change → no re-review per the contract's versioning rule). `contracts/shared/telemetry_schema.json` (JSON Schema draft 2020-12, additionalProperties:true for minor-forward-compat + required lists), canonical golden fixtures `contracts/shared/telemetry_examples/*.json`, reference validator `scripts/validate_telemetry.py` (schema + D13 identities + per-source conservation + finiteness), and a CI step validating the examples each PR. Producer/consumer obligation added to the contract: validate against the schema in tests. Importable validator utilities (Python `energy_go.telemetry.validate`, frontend TS module) are delegated implementation tasks wrapping this schema+examples. (PR #20)
- [D19] 2026-06-10 — §4.2 load-generator parameters are **×100 the literal spec values**: base = 75 000 kW (75 MW), α = 4 500 kW/°C, β = 3 750 kW/°C, σ_AR1 = 5 000 kW. The literal §4.2 figures (base 750 kW, α 45, β 37.5, σ 50 → ~1.5–2 MW) contradict (i) the §1/§3 stated Gansu site load of 50–100 MW, (ii) the §2.1 normalization `load_kw/100000` (which only yields an O(0.5–1.0) signal at 50–100 MW; 75 000/100 000 = 0.75), and (iii) the demand-charge scale. ×100 reconciles all three; it is a unit-scale error in §4.2, resolved here as a binding interpretation (same mechanism as D3/D5 for spec-internal contradictions). Applied in BOTH the JAX core and the NumPy reference (D11); Gansu parity uses these values. Follow-up: a §4.2 spec-text correction PR (human-gated) will make the spec self-consistent, but does NOT block — this DECISION is authoritative and unblocks the reference-impl gate (PR #14, task #5) and task #8. Resolves backend-reviewer/jax-env gate blocker B4. (decision PR; cited on PR #14)
- [D20] 2026-06-10 — `tests/frontend_e2e/` area created for Playwright E2E browser tests (USER task #29), categorically distinct from `tests/frontend/` Vitest+RTL unit tests. E2E files are named `<scenario>.spec.ts` (Playwright convention — no JSX/RTL; visually distinct from `.test.tsx`); `<scenario>` is the E2E scenario (e.g. `smoke`), NOT required to match a component/contract filename (that match rule is unit-test-only). Harness contract `contracts/frontend/playwright_harness.md` (frontend area → frontend-reviewer gates). Same mechanism as D15 (a test area comes into existence by DECISION when its first feature lands). CLAUDE.md naming + check_conventions.sh updated to document/enforce the area. (decision PR)
- [D21] 2026-06-10 — Refines D10 "terminal flush": the flush books the **final complete calendar month of a full-year accounting horizon**, NOT a per-training-episode flush. `c_demand_charge` (real money) is booked at each calendar-month boundary and the final month at the full-year eval's terminal step (`t==8759` for the 8760-step eval, D3). A 7-day random-start training episode (D3) that completes no calendar month books **0 real `c_demand_charge` by design** — correct, not a gap: (a) a 7-day slice is not a billing period; (b) the training demand-pressure learning signal is the reward's per-step `2·c_demand_shape` (D13 reward-basis), not `c_demand_charge`; (c) booking a full `month_peak·rate` on a 7-day slice ~4× over-weights real demand charge and pro-rating is arbitrary. The reference impl (book at month boundary OR `t==8759`) is therefore conformant — no booking-logic or `is_last_step`/`done` signature change; `c_demand_charge` stays OUT of `cost_total_reward_basis` (D13 preserved). Tests: 7-day episode asserts `c_demand_charge_yuan_cum == 0`; full-year path books each month exactly once (M2 two-month Σ = no double-count). Resolves the PR #14 round-5 D10-semantics blocker. (decision PR; cited on PR #14)

- [D22] 2026-06-10 — Post-relaunch triage rulings (rl-architect; task #9):
  **(a) The D11 NumPy reference implementation is COMPLETE — no further reference task is owed.** PR #14 landed the full from-scratch reference (`src/reference/{gansu_env,gansu_params,tariff}.py`), the §3/§4 reference-conformance tests, and the reference self-consistency parity suite (Suite 1 of `tests/env/test_env_parity_gansu.py`, which always runs), and closed the gate (latest markers: backend-reviewer APPROVE, team-lead APPROVE, qa-engineer QA_PASS). The PR title "contract + invariant helpers" understates it; it was a combined contract+tests+implementation PR taken all the way through QA_PASS.
  **(b) The JAX↔reference parity (Suite 2 of `tests/env/test_env_parity_gansu.py`, gated by `importorskip` of `energy_go.env.jax_env`) is the binding QA acceptance gate for `jax_env_core` (PR #33), NOT a separate deliverable.** `jax_env_core` may not receive QA_PASS until Suite 2 is no longer skipped and passes at rel-tol 1e-4 (single-step, SOC-trajectory, obs, vmap, jit, and 8760-step cumulative-reward parity). The `jax_env_core` contract MUST name the import path `energy_go.env.jax_env` (matches the parity test) and its Definition-of-Done MUST include "Suite 2 green."
  **(c) Next-contract sequence** (REBUILD_SPEC build order; §3 baseline now done): the JAX env-step API (PR #33 contract) is the critical-path lynchpin. Once PR #33's contract+tests gate is APPROVED, the training-pipeline §5 contract and the env-harness contract may be authored in parallel against the locked env-step signature (their *implementations* still wait on the env-core implementation). §10 Tier-1 E2/E5 remain DEFERRED until JAX baseline parity is established (per D17), default-OFF toggles.
  **(d) Checkpoint format is the next shared contract to LOCK** (producer: training-engineer; consumer: serving policy-export / ONNX): actor weights + running normalization stats. Owner training-engineer drafts `contracts/training/checkpoint_format.md` once the env-step API contract is APPROVED; shared → both reviewers comment, rl-architect locks (CLAUDE.md shared-contract routing). This prevents the training producer and serving consumer from diverging on the on-disk format.
  Task #8 (`telemetry_validate` Python impl) routing is NOT a new decision — the merged review record (`contracts/reviews/telemetry_validate.md`) already fixes the gate at **backend-reviewer** (frontend-reviewer advisory); recommended implementer serving-engineer. (meta/decision PR)

- [D23] 2026-06-10 — **The §8 asset/visual side (GLB models + registry entries) is CLEARED to start now; it is NOT gated by the §8 env-side "after baseline parity" rule (D2).** D2's "§3 plant first, §8 after baseline parity" gates the §8 **env-logic** — composable obs/action derivation and gas/electrolyzer physics that could destabilize Gansu env parity — NOT the 3D asset library, which has zero dependency on env physics. Additive registry entries are a minor (v1.0.x) bump with **no re-LOCK** per the registry v1.0.0 LOCK. **Binding guardrail:** the registry key = config-YAML asset ID verbatim is a LOCKED cross-area invariant; since the §8 env-side config does not exist yet, new asset IDs MUST be derived from REBUILD_SPEC §8's named asset taxonomy (gas combustion, PEM electrolyzer, alkaline electrolyzer, the 6 load archetypes), in the same id format as the 4 Gansu entries (`^[a-z0-9][a-z0-9.-]*$`), NOT invented ad hoc — so the eventual §8 site-config YAML consumes these exact IDs; any §8 asset the spec has not concretely named is flagged provisional in the contract, not guessed. The 4 LOCKED Gansu entries are not touched. **Area/gate:** `contracts/assets/` (consistent with `registry_schema.md`), frontend-reviewer gates (3D visual domain), contract-first. rl-architect re-engages only if an entry would change/remove a LOCKED field (→ breaking, superseding DECISION + re-LOCK). Owner 3d-assets-engineer. Clarifies D2; does not supersede it. (meta/decision PR)

- [D24] 2026-06-10 — **Inference-stream Replay-speed semantics reconciled (resolves a `contracts/serving/inference_stream.md` internal contradiction).** The contract's Replay-speed section was self-inconsistent: the prose said default = "no artificial throttle (stream as fast as the env step completes)", while `speed = 1.0 (default)` = "one simulated hour per real second (1 Hz)" — i.e. a 1 s/frame throttle. **Binding interpretation:** the `start` command's `speed` field **defaults to `1.0` (1 Hz)**; "no artificial throttle" means *no throttle beyond the `speed` control* (no separate WS-buffer/queue throttle) — the two sentences are reconciled, not in conflict. Inter-frame sleep = `0.0 if speed == 0 else 1.0 / speed`; `speed` clamped to `[0, 100]`, negative → 0; `speed = 0` is genuinely uncapped ("as fast as possible"). The `speed` field MUST be implemented (it was found entirely unimplemented during PR #29 review — start handler didn't parse it, `_step_loop` didn't honor it; not in the contract's "Out of scope" list, so it is in scope). A hardcoded unconditional inter-frame sleep (e.g. `asyncio.sleep(0.001)` to satisfy test-thread scheduling) is **rejected** — it contradicts `speed = 0` and couples production timing to the test harness; timing-sensitive WS tests instead pass a finite `speed` (e.g. 100 → 10 ms/frame) as a documented knob. The one-line contract-text correction lands in PR #29 alongside the `speed` implementation (serving area → **backend-reviewer** reviews it there); this DECISION is the authoritative interpretation and unblocks the implementation now. Owner serving-engineer (impl, task #18); resolves the PR #29 `002c783` REQUEST_CHANGES. (meta/decision PR)

- [D25] 2026-06-10 — **The checkpoint-format contract is a SHARED contract → it lives at `contracts/shared/checkpoint_format.md`, NOT `contracts/training/`.** Corrects the path detail in D22d, which wrote `contracts/training/checkpoint_format.md`; the *substance* of D22d is unchanged (checkpoint format = the next shared contract to LOCK: actor weights + running normalization stats; producer training-engineer, consumer serving policy-export/ONNX). It is a genuine cross-area producer/consumer format, exactly the `contracts/shared/telemetry_schema.md` precedent, and per D15 a shared-area contract's tests live under `tests/shared/` (e.g. `tests/shared/test_shared_checkpoint_format.py`). **Routing = shared:** both reviewers comment (advisory, not a gate), rl-architect LOCKs on its own authority after the contract+tests gate settles — same flow as telemetry. PR #41 moves accordingly. (Training-area contracts such as the §5 training pipeline, PR #40, stay in backend-reviewer's normal gate — they are not shared.) Supersedes the path detail of D22d only. (meta/decision PR)
- [D26] 2026-06-10 — **Telemetry-producer validation is a two-tier obligation (generalizes D18; clarifies, does not change it).** Resolves task #14 (the deferred reconciliation of the inference_stream contract's "MUST assert `== []`" against the live-stream resilience already in the implementation). Every telemetry producer (`serving.inference_stream`, `serving.training_proxy`, the env harness, the future real-env serving cutover) MUST call `energy_go.telemetry.validate(msg)` before sending, with **two tiers**: (1) **Test-time = hard gate** — every test that receives a frame MUST `assert validate(msg) == []`; a non-empty list in tests is a producer bug (this is the systematic-failure gate). (2) **Runtime = resilient** — in a live stream a validation failure MUST be logged as a structured warning (fields `kind`, `seq`, error-list) and MUST NOT crash the session **nor silently drop the frame** (resilience-first: the frame is still sent; consumers validate independently — e.g. the frontend telemetry_validator — as defense in depth). Rationale: a transient physics NaN in a multi-hour live session must not terminate the connection; the test-time gate catches systematic bugs before production. This does NOT weaken D18 (the test-time assert is unchanged) and does not touch the LOCKED telemetry schema (which mandates test-time validation, not runtime crash). The implementation was already conformant (`_send_validated` "never blocks or raises"); no code change. **Contract-text application:** the serving wording lands in `contracts/serving/inference_stream.md` via PR #46 (serving area → **backend-reviewer** gates the text); env-harness and real-env-serving (task #23) contracts inherit and must reference D26 rather than re-litigate per producer. (meta/decision PR)
- [D27] 2026-06-10 — **Training loop MUST be device-resident (enforces spec §7); a host-side replay buffer is DENIED for the §5 training pipeline.** Ruling on backend-reviewer's PR #40 blocker F2 (rl-architect's call between device-residency vs an rl-architect-signed host-buffer v1 deviation). **Deviation denied; F2 affirmed.** Spec §7 is explicit and a-priori, not a measurable trade-off: "End-to-end on device: env, replay buffer, and SAC update all live on GPU — **zero host↔device copies per step** (this is what kills SB3+Gym setups)"; target **10⁶–10⁷ env-steps/sec** (vs ~9k CPU); names `flashbax` as the buffer building block. The flagged host-side NumPy buffer with per-step host↔device copies + per-grad-step `float(...)` syncs is the exact anti-pattern §7 names — it is the rebuild's entire raison d'être to eliminate it, so there is nothing to "relax." (A deviation would be a costly-to-reverse, cross-area architecture change → per CLAUDE.md it would itself require human-user escalation; the bar is not met — enforcing the spec, by contrast, is rl-architect's own authority.) **Acceptance criteria for §6.2/§7 conformance (binding for the §5 training-pipeline merge):** (1) device-resident jittable replay buffer (`flashbax` or equivalent fixed-size buffer; no host NumPy buffer in the loop); (2) env `vmap` step + buffer insert + SAC update inside a single jitted region, gradient steps via `lax.scan`/`fori_loop`; (3) zero per-step host↔device copies, no per-grad-step `float()` telemetry syncs; (4) on-device telemetry accumulation, `device_get` only at the telemetry/eval cadence. The requirement is structural and does NOT wait on benchmarking (which needs PR #33 merged); correctness-first-on-host-buffer-then-retrofit is the costly path. Adding the buffer lib requires a STACK.md entry in the same PR. PR #40 F1 (`penalty_yuan` field-name bug) fixes regardless. backend-reviewer re-gates on this basis. (decision PR; cited on PR #40 comment 4676489497) (meta/decision PR)
- [D28] 2026-06-10 — **`checkpoint_format.md` §6 `actor_forward_numpy` clips `mean` to ±8.0 before the tanh/sigmoid squash (patch clarification of the LOCKED v1.0.0 contract; stored format UNCHANGED).** Float32 `tanh`/`sigmoid` saturate to exactly ±1.0 / 0.0 / 1.0 for `|x| ≳ 8.7`, which violated §6's already-stated OPEN ranges (`a_bat ∈ (-1,1)`, `fractions ∈ (0,1)`) AND diverged from the JAX `actor_forward` (which already clips ±8.0, backend-reviewer-approved on PR #40) — breaking §6's own `atol=1e-5` producer/consumer parity requirement. The clip reconciles the recipe with its own guarantees; it adds no key and changes no shape/dtype/`schema_version` (stays `"1.0.0"`). **Authority:** this is a §6 *recipe* clarification toward the existing guarantee, not a format/guarantee alteration and not costly-to-reverse → within rl-architect's own authority (the LOCK's versioning rule pre-authorizes patch/doc clarifications); NOT a human-escalation case (only spec changes / irreversible / actual format alterations escalate). The ±8.0 bound is a binding cross-impl invariant: the §6 numpy recipe and the JAX `actor_forward` MUST use the identical bound for parity. rl-architect authored the §6 contract edit (locker authority); training-engineer updates `test_manual_forward_matches_api` to add the same clip (backend-reviewer reviews the test edit). Consumers: serving (real-checkpoint cutover, task #23) implements §6 incl. the clip. (meta/decision PR; resolves the PR #40 checkpoint-parity test)

- [D29] 2026-06-11 — **`check_pr_gate.sh` head-coverage rule: gate-deciding markers MUST post-date the head commit (backfilled — the rule landed as chore PR #63, task #36, without a formal DECISION entry).** A verdict only counts if it covers the *current* head: the gate (`scripts/check_pr_gate.sh`) reads the head commit's `committedDate` and BLOCKS if any gate-deciding marker (required-reviewer APPROVE / QA verdict) is older — a commit pushed after an APPROVE/QA marker forces re-review/re-QA at the new head. Resolves the PR #10 merge-incident root cause (a commit pushed after the markers slipped through a gate that didn't check head-coverage). **Limitation (committedDate vs pushedDate):** a force-push of an old commit can present a stale `committedDate`; the secondary guard is a **SHA-citation-mismatch WARNING** (a marker scoping a SHA ≠ current head prints a warning, never hard-blocks, to stay fail-open). Implemented in `check_pr_gate.sh` (PR #63) + a pipe-tripwire header (PR #73, "the exit code IS the gate"); tested in `tests/meta/test_meta_check_pr_gate.sh` (gh-stub, bash-3.2). The rule has been enforced since PR #63; this fills the D-number it was owed. (backfill meta PR)

- [D31] 2026-06-11 — **Master plan APPROVED at the Fable gate (team-lead delegated authority); records the binding architecture + rulings + the still-open USER questions.** `docs/design/master_plan_geo_finance.md` merged at `22a8813` (PR #78); §12 weather design study at `b0ae5fd` (PR #77). The plan is the coordination architecture for the 5 forward workstreams (A geo-UI · B device-physics schema · C multi-year sim · D project finance · E policy-selector economics UI). Binding elements:
  **(a) Keystone — device-model ID = universal join key** (one ID, three facets: visual/`registry.json` · physics/B · economics/D), exploiting the LOCKED registry's key==config-ID invariant. B is foundational; now realized by the LOCKED `device_model_schema` v1.0.0 (see Locked section). §9-1 ENDORSED, §9-2 CONFIRMED (`config/device_models.yaml`, 3-0 vote: rl-architect/frontend-reviewer/jax-env).
  **(b) Scenario-as-configuration** (BINDING, USER directive): a scenario is a *configuration, not a mode* — it composes the economic+objective layer as pure config over the same device-model IDs. **v1 ships the power-supply scenario END-TO-END ONLY.** The hydrogen / aluminum / data-center-token scenarios are **design-proven config-only validation cases, NOT v1 deliverables**. The **flexible/shiftable-load action extension** (data-center deferrable jobs — the one future scenario that touches the *action* space, not just config) is explicitly **NOT v1**. Also NOT v1: the full §8 device library beyond Gansu's 4, non-Gansu checkpoints, `lax.scan`-across-years, the Monte-Carlo ensemble (finance ships M=1 with the ensemble schema in place), API-key map-tile providers.
  **(c) Foundation-first sequencing:** B → A/C → D → E (§9-1, §9-3 thin-vertical-slice CONFIRMED: B + Gansu-parity first, then power-supply end-to-end). B implementation may spawn immediately (critical path); D/E implementation waits on the bound USER answers in (g).
  **(d) C (multi-year) is §7-clean — D27 does NOT apply.** A 10/20-yr run = `Y` jitted 8760-step dispatch years with cheap **host-side per-year param updates** (degraded capacity/efficiency, escalated prices = closure constants per year). This is an **eval workload**; D27's "zero host↔device copies per step" governs the 10⁷-step *training inner loop* only — the year boundary is the natural host point (D21 calendar-flush / env-harness eval precedent). Which policies run multi-year: **BOTH the RL policy and all §11 baselines** (per-policy comparison demands identical treatment). Replacement: **auto-at-EOL default with user-schedule override**.
  **(e) F1 — constant-real-price dispatch default (contract-stage must-resolve; binds C/D).** Uniform nominal escalation interacts with **policy distribution drift**: a policy trained at year-1 price levels faces ~49% higher absolute prices by year 20 at 2%/yr → observations drift off the training distribution, even though *uniform* escalation leaves relative TOU structure (hence optimal dispatch) unchanged. **Binding default: simulate dispatch at CONSTANT REAL (year-1) prices, and apply escalation purely in the finance layer post-hoc** — financially equivalent under uniform escalation, and it keeps the policy on-distribution. **Non-uniform (stream-specific) escalation genuinely changes dispatch incentives** → requires retraining OR explicit limitation acknowledgment. finance-expert + jax-env-engineer resolve the mechanism in the C/D contracts. (Cite as D31/F1.)
  **(f) Finance (D) rulings + off-wire boundary.** Finance composes on top of the LOCKED **D13 real-money** identity (operating cost → project cash flow) as a **separate REST resource** `GET /api/finance/compare`, joined to operating runs by `(policy-id, checkpoint-id, scenario-id)` — **OFF** the LOCKED per-step `eval_compare` wire (no telemetry change; any finance term on the wire would be an additive-minor bump + both-reviewer re-review, deliberately avoided). §5.13 rulings within delegated authority: **memo-vs-cash** CONFIRMED (degradation/curtail/VOLL memo-only via INV-DEG/INV-CURT/INV-VOLL; wear's cash impact only as replacement-CAPEX; INV-BASIS); **base case pre-tax, all-equity unlevered** (tax/debt as default-off toggles); **dual View I/II** with View II (incremental-vs-no-battery) the per-policy headline; **P50/P90/P99 exceedance**, v1 M=1 with ensemble schema ready; **¥ RMB nominal** accounting basis (with F1's real-basis *dispatch* note); **MIRR alongside IRR** (replacement-year sign flips). Extended `PolicyEvalResult` (per-stream + physical-quantity hourly accumulators) is the D prerequisite (task #55).
  **(g) Still OPEN with the USER (gate only the *bound* parts of D/E; do NOT block B):** (1) **WACC** default + sweep range (China utility-scale ≈ nominal 7–9% / real 5–7%); (2) **horizon** emphasis (20-yr base + 10-yr variant); (3) **v1 revenue-stream set** for the user's actual site; (4) **shipped econ defaults** (Chinese 2024/25 benchmark CAPEX/OPEX) vs fully-configurable-no-defaults. Separately, the **§13 project-finance SPEC section** (finance-expert authors) goes to the USER as a **REBUILD_SPEC change → human-gated** (§9-4 CONFIRMED); task #56's §5.7 econ-field alignment folds into it.
  Tasks spawned/aligned: #54 (B, LOCKED), #55 (extended PolicyEvalResult), #56 (§5.7→§13 alignment), #57 (v1.1.0 wear/lifetime, C/D pre-gate). (meta/decision PR — rl-architect authority; records a merged, team-lead-ruled plan)

- [D32] 2026-06-12 — **Product spine + scenario activation — the unified `config → algorithm → train → eval → finance` pipeline (binding; indexes the merged `docs/design/product_spine.md`, PR #84 @ `c79b88e`).** Consolidates the 2026-06-12 USER product directives (relayed via team-lead delegated authority) on top of D31. Binding elements:
  **(a) Five-stage spine:** ① config (geo/devices/tariff/scenario **+ fleet sizing**) → ② **select algorithm** → ③ train → ④ eval → ⑤ project-finance simulation. The frontend's primary UX is a guided wizard mirroring these five stages (A+E). v1 ships the **power-composite** scenario end-to-end through the whole spine FIRST.
  **(b) Single-config invariant** (binding C/D/E acceptance criterion): **one per-step stream-economics computation, three consumers** — the reward, #82's stream-keyed eval accumulators, and the finance engine — so `reward streams ≡ eval-accumulator streams ≡ finance streams` (single source, no dialect divergence). The scenario config (+ algorithm config) is the **sole** parameterization; join keys `device-ID · region-ID · stream-ID · checkpoint-ID · algorithm-ID` thread every stage. No stage may require a config field the others don't share.
  **(c) Algorithm registry:** `algorithm_id` is a stage-② config field → a code registry of **policy producers**; SAC (trains) + the §11 baselines (NoBattery/TOU/DP-oracle — produce a policy *without* training) + future PPO/TD3 **unify** as producers whose output is a uniform **policy artifact**; stages ④⑤ consume it **algorithm-agnostically**.
  **(d) Scenarios COMPOSE — a scenario is a SET of enabled device/stream groups on the power base, NOT an enum** (USER ruling). Config is set/flags; **no contract may bake in mutual exclusivity**. obs/action compose **additively at fixed canonical offsets** (deterministic group ordering for checkpoint comparability); the policy↔env compat check keys on the **enabled-SET** (read from the checkpoint's `run_config_json` — already in the LOCKED `checkpoint_format`, **no re-LOCK**). Dispatch competition (sell power vs make H₂ vs serve datacenter) is the RL policy's job via the unified reward — no extra mechanism. This retroactively validates the #82 pre-declared-6-streams decision (enabling a group flips its stream dormant→active).
  **(e) Scenario activation — SCHEDULED (no longer deferred-indefinitely), per group, gated.** Sequence by increasing kernel-internal novelty: **power-composite v1 → H₂ → aluminum → datacenter**. The activation gate is unchanged (action_dim growth, new state, §3.6 extension, per-enabled-set `(obs_dim,action_dim)` → own checkpoint, both-reviewer re-review + rl-architect re-LOCK); only the schedule changes. The **§3.6 enforcement extension is staged**: per-step DEVICE-ENVELOPE clips (H₂ — tractable) → multi-step/TEMPORAL constraints (aluminum max-outage — harder) → flexible-load ACTION extension (datacenter). **Two FUTURE USER sign-offs** flagged (escalate at design time, not now): the aluminum temporal-§3.6 (a REBUILD_SPEC §3 amendment) and the datacenter flexible-load action model.
  **(f) Reward alignment:** D13 **extends** (does not break) — `reward_basis = Σ(active real-money streams, cash-flow-signed) + bounded shaping`, real-money/shaping separation preserved; non-power scenarios gain product-revenue streams so the policy maximizes project value ("train on the economics you'll be judged on"). **F1-consistent** (year-1 real prices in dispatch; finance escalates post-hoc). Kernel-internal → part of each scenario's activation gate.
  **(g) Train/eval DECOUPLE via an immutable policy library** (USER revision). Stage ③ deposits immutable policy artifacts (provenance: config-hash · algorithm_id · train-date · step); stage ④ is **selection** (policy × env config), multiple eval results coexist; ⑤ consumes a selected one. **Policy↔env compatibility check** (backend single-source, surfaced in the picker greyed+reason); **cross-eval provenance** (`trained-on` vs `evaluated-on` — a flagged robustness feature, `dispatch_fidelity` guard family).
  **(h) Fleet sizing configurable** (schema already carries it, v1.0.0 — A UI requirement, not a schema change); finance **CAPEX = units × unit-price** (#63). **Stage-invalidation DAG:** training runs are **immutable** (a config edit never stales a run); invalidation applies to the **eval→finance** edge and **config→(new evals)**, NOT `config→runs`. **Physical config** edits (sizing/device/tariff-shape/algorithm) need a new eval (retrain if no compatible policy); **finance-only** edits (discount/escalation/currency) re-slice ⑤ instantly — the **F1 zero-dispatch dividend** realized as interactive UX. Stale edges are machine-visible (provenance-guard family).
  **(i) Config validation** is a two-tier (hard-errors-reject / warnings-proceed) **sibling shared contract `config_validation`** (task #66) — ALL rules in the resolver layer exactly once; serving exposes a `validate` endpoint; UI renders field-level, no duplicate TS rules; physics (jax-env) + econ (finance-expert) rules jointly.
  Authority: rl-architect authored the spine doc (#84) under USER directives via team-lead delegated authority; this entry is the binding index. Tasks: #64 (this), #66 (config_validation), #67 (workbench, v1.1), #68 (grid-tech + private overlay, v1.1). v1.1 spine items (grid-tech config-variant compare · workbench tiering · two eval modes · private overlay) land as a v1.1 amendment, not a re-open of the frozen v1.0 doc. (meta/decision PR — rl-architect authority; records the merged spine)
- [D34] 2026-06-13 — **Finance ensemble default M=50; downside-tail decision percentile = P95 — USER-CONFIRMED override of D31's v1 M=1 guard** (co-authored rl-architect + finance-expert; realized by §13 / PR #100; lands on #100 merge). Supersedes D31 §7.1 v1-scope + ruling-(f) "v1 M=1" clauses ONLY; the rest of D31 stands. **Binding:** **M=50 default** weather draws (§12 block-bootstrap) → genuine exceedance distributions. **P50/P75/P90/P95 are statistically sound at M=50; the headline/decision downside-tail percentile = P95** (USER call). **P99 is DROPPED from the headline** (≈0.5th-worst of 50 → not credible; optional indicative-only if shown at all; a credible P99 gates on M≥100) — the P99-weakness analysis (rl-architect) drove the P95 selection. **M=1 retained as a valid fast-iteration mode with distributional metrics SUPPRESSED** via `distribution_valid` (percentile/downside fields explicitly absent, never fabricated; schema identical across M — honest collapse, §13.10c); relative ranking robust at M=1 under CRN. **Common Random Numbers (binding):** all policies in a comparison consume the SAME M draws (shared seed in provenance) → per-policy deltas are pure dispatch, not weather noise. **Axis separation (INV-FINLAYER, §13.4):** M = the only stochastic axis (→ the one exceedance distribution); price paths = a deterministic sensitivity axis; never cross-producted; M=50 grows the weather axis only. **Consequence:** §12 block-bootstrap + its PR#77 §4.2 validation battery are now a **v1 finance PREREQUISITE** (a design study under D31); the P90/P95 numbers are only as valid as the ensemble's statistical fidelity; workstream-C vmaps 50× over the M axis. **Scope:** D34 records the ensemble decision (M=50 / P95) ONLY. It does not bind the CAPM discount-rate default values (USER-confirmed separately in §13.5b, `497e446`) nor the econ-defaults-from-#63 decision. (meta/decision — co-authored rl-architect + finance-expert; records the merged §13 ensemble override.)
- [D36] 2026-06-13 — **§13 Project-Finance spec ratified — the canonical finance layer (stage ⑤ of the product spine), USER-signed-off; indexes the merged §13 + its §13.13 resolved decision set (`docs/spec/section_13_project_finance.md`, PR #100 @ `497e446`).** §13 turns a dispatched operating result (the D13 real-money `PolicyEvalResult`) into investor/lender-grade economics — NPV/IRR/MIRR/LCOE/LCOS/payback as **distributions** over a weather ensemble, a **downside-risk centerpiece**, and a **price-path scenario library**. The engine is **off-wire, offline, and pure** (composes on the LOCKED D13 identity; the `eval_compare` wire is untouched; exposed as the new `GET /api/finance/compare` REST resource — a thin wrapper over the pure `finance(ensemble, price_paths, econ, finance_config) -> FinanceResult` boundary backend-reviewer gates). **Binding elements (§13.13 resolved set):** (1) ensemble **M=50 / P95** → recorded separately as **D34** (supersedes D31's M=1). (2) **Downside-risk 6-metric centerpiece** (worst-case NPV, max drawdown+yr, P(NPV<0), P(IRR<hurdle), CVaR-5%, worst single-year CF). (3) P95 tail, P99 dropped from headline (→D34). (4) **INV-FINLAYER** — new named binding invariant: price paths / escalation / contract structures are a finance-layer-only post-hoc transform over constant-real year-1 dispatched streams, **structurally barred from dispatch** (F1); non-uniform paths raise an explicit `requires_retrain` flag; the two distributional axes (M weather = stochastic Monte-Carlo; price paths = deterministic sensitivity) are **never cross-producted**. (5) **CAPM discount rate** — methodology + **USER-CONFIRMED default values LOCKED in §13.5b** (β_U 0.60, ERP 6.0% +CRP 0, cost-of-debt 5yr-LPR+125bps, D/E 0 unlevered & 1.5 levered, tax 25%, VAT-out, linear-interp tenor, static curve v1; provenance USER-confirmed/2026-06-13). (6) Horizon **20-yr primary + 10-yr variant**. (7) Lifecycle replacement **first-of(10-yr, cycle-life)** + named asset-management lines. (8) v1 revenue = **composite** of active power-supply streams (h2/avoided-cost/token design-proven config-only). (9) Carries forward the D31 base rulings (INV-BASIS/DEG/CURT/VOLL, dual View I/II, MIRR alongside IRR, ¥-nominal, off-wire REST). (10) Econ defaults sourced from the **#63 benchmark library**. **v1 prerequisites:** extended `PolicyEvalResult` (task #55) + §12 block-bootstrap @ M=50 (PR#77 §4.2 battery). **Authority:** USER-signed-off REBUILD_SPEC addition (surfaced via team-lead); this entry is the binding index. Owner finance-expert (impl: finance-engineer). (meta/decision — rl-architect authority; records the merged §13.)

- [D33] 2026-06-13 — **E-TAR-SHAPE relaxed to accept flat `(24,)` OR seasonal `(12,24)` under `device_model_schema` v2.0+ — live-bug fix (PR #101).** The v2.0.0 `price_table` reshape `(24,)→(12,24)` (PR #87) left `config_validation`'s E-TAR-SHAPE v2 branch requiring a strict `(12,24)`, but `config/site_gansu.yaml` carries a flat `(24,)` tariff that `resolver.py` accepts and broadcasts ×12 — so `validate(site_gansu, device_models@2.0.0)` emitted a spurious `E-TAR-SHAPE` hard error and `resolve_site()` **raised on the flagship site** (live-broken end-to-end). CI stayed green only because the unit fixture was pinned at schema 1.1.0 — the validator-vs-live-file version drift that hid it. **Fix:** under v2.0+, E-TAR-SHAPE accepts flat `(24,)` OR `(12,24)` (HARD ERROR iff shape ∉ {`(24,)`, `(12,24)`}); the `resolver.py` inline path also accepts a `(12,24)` passthrough, so both sides accept exactly `{(24,), (12,24)}`. **Binding principle — the validator follows the resolver:** the resolver is the single source of truth for the accepted config surface (D18 single-validator, D26 two-tier), so the validator must never reject what the resolver accepts/broadcasts nor accept what it would raise on (validator↔resolver accepted-shape **parity**, guarded by `test_real_gansu_config_validates_no_tar_shape` against the on-disk configs + `test_resolver_inline_seasonal_passthrough`). **MINOR revision** of LOCKED `config_validation` v1.0.0 (relaxes an existing rule's accepted set; removes/renames/retypes nothing; `ValidationResult`/`ValidationIssue` shape unchanged) → **no re-LOCK**, rl-architect authority, normal backend-reviewer + QA gate, no USER escalation. Realized by PR #101. Cite as D33.

## Locked shared contracts

- [LOCKED] 2026-06-10 — `contracts/shared/telemetry_schema.md` v1.0.0 (PR #6). APPROVED by backend-reviewer and frontend-reviewer (VERDICT comments on PR #6; `gh pr review --approve` 422s on same-account PRs, so the VERDICT-marker comment is the durable record per task #12). The stable wire format for `env_step` / `train_metrics` / `eval_compare`. Embeds D3–D13. Consumers (dashboard, 3D scene) and producers (env harness, training loop, serving) implement against it; any field removal/rename/retype or unit change requires a superseding DECISION + re-review by both reviewers (semver: additive=minor, breaking=major).
- [LOCKED] 2026-06-10 — `assets/3d/registry.json` v1.0.0 (PR #24; schema contract `contracts/assets/registry_schema.md`). rl-architect LOCK; advisory review window given to both reviewers (input advisory, not a gate, per CLAUDE.md shared-contract routing). Keyed-by-id object `{ "schema_version", "assets": { "<id>": {path,type,dims_m,pivot,animation_hooks} } }` — the map key IS the asset ID, **verbatim equal to the config-YAML / SiteSceneConfig asset ID** (binding cross-area invariant; key format `^[a-z0-9][a-z0-9.-]*$`). Scene code resolves ALL asset paths via `resolveAsset(registry,id)=registry.assets[id]` (no hardcoded paths). 4 Gansu parity entries (vestas-v150-4.2, trina-vertex-n-670w, catl-lmp-300mwh, pcc-substation-945mw). Additive entries (new §8 GLB models) = minor bump, no re-LOCK; field removal/rename/retype = breaking → superseding DECISION + re-LOCK + re-review. Consumers: frontend3d scene; refs: config/site_gansu.yaml asset IDs (+ any serving asset metadata).
- [LOCKED] 2026-06-10 — `contracts/shared/checkpoint_format.md` v1.0.0 (PR #41, commit 2758088; D22d/D25). rl-architect LOCK on own authority (shared routing; both reviewers' advisory COMMENTs addressed). The single-artifact `.npz` checkpoint carrying actor MLP weights + VecNormalize obs stats; loadable without importing `energy_go.training`/JAX/Flax. **Verified at lock time:** (a) **spec-conformant** — `obs_dim=107` (§2.1: 11 base + 24×4 forecast) and `action_dim=6` (§2.2 Energy Router) match the spec exactly; 6-dim threaded consistently (actor out `Dense(12)=mean(6)+log_std_raw(6)`, critic input `113=107+6`, `target_entropy=-6`); (b) **producer-consistent** — actor architecture (Input107→Dense256 ReLU→Dense256 ReLU→Dense12; `tanh(mean[0])`+`sigmoid(mean[1:6])` squash; `log_std=clip(·,-5,2)`) matches `contracts/training/training_pipeline.md` §5.2 (PR #40) byte-for-byte, incl. action semantics; (c) **§8.1 join key** `checkpoint_id` is consistent with the LOCKED telemetry schema (`eval_compare` Kind 3 + `env_step` carry it). Keys: flat `actor_<layer>_<param>` (`(in,out)` order for `y=x@W+b`), `obs_mean/obs_var/obs_count/obs_clip` (norm recipe `std=sqrt(var+1e-8)`, clip), metadata (`checkpoint_id` UUIDv4, `run_id`, `global_step`, `created_at_utc`, `code_version`, `run_config_json`), optional critic keys for resume. API: `save_checkpoint`/`load_checkpoint` only (atomic same-dir write; major-version load reject). Versioning: additive=minor, removal/rename/retype/algorithm-change=major → superseding DECISION + re-LOCK. **Binding consumer-conformance note:** the *merged* serving policy path (`src/energy_go/serving/inference_stream.py` `_load_policy`/`policy_forward`/`_normalize`, PR #29) is a `_SyntheticEnv` PLACEHOLDER and is **NON-CONFORMANT** to this format (uses `w_0…w_N` keys, tanh-hidden+identity-output, `obs_std` from a separate file). It MUST be replaced with `load_checkpoint()` + the §6 `actor_forward_numpy` recipe at the real-checkpoint cutover (task #23; relates to real-env serving integration, task #14). This is a tracked fix-forward, NOT a lock blocker — the contract is the authority and the placeholder predates it. Consumers: serving (inference/export), env-harness (eval/replay), training (resume). Producer: training-engineer.
- [LOCKED] 2026-06-11 — `contracts/shared/device_model_schema.md` v1.0.0 (PR #79, head commit 3ff05ee; master-plan keystone, task #54). rl-architect LOCK on own authority (shared routing; both reviewers' advisory COMMENTs in — frontend-reviewer + backend-reviewer). The device-physics schema (`config/device_models.yaml`, sibling of `site_*.yaml`) keyed by the existing asset/device-model IDs, plus the pure-Python resolver `resolve_site(site_path, device_models_path) -> (EnvParams, obs_dim, action_dim)` that composes a site's fleet against device models to build the env's `EnvParams` pytree (the `PRICE_TABLE_YPW` module constant is folded into `EnvParams.price_table`, a `(24,)` array). **The device-model ID is hereby a binding cross-area join key** spanning three facets of the same physical device: **visual** (`assets/3d/registry.json` key, LOCKED), **physics** (this schema's `physics:` block), **economics** (Workstream-D's `economics:` block, off-wire). All facets resolve by the SAME verbatim ID (`^[a-z0-9][a-z0-9.-]*$`); the 4 Gansu IDs (`vestas-v150-4.2`, `trina-vertex-n-670w`, `catl-lmp-300mwh`, `pcc-substation-945mw`) are the parity set. **Composition rule (LOCKED):** `physics:` constants are **non-overridable** by a site (per-device intrinsics); `fleet:` quantities (unit counts, capacities, site wiring) are site-**overridable**. **Verified at lock time at head 3ff05ee, field-by-field:** (a) **parity half — complete + future-proof** — `test_all_envparams_scalar_fields_parity` parametrized over `EnvParams._fields` (auto-covers every present and future scalar field; `resolve_gansu()[0] == EnvParams()` bit-exact via `pytest.approx(rel=1e-9)`), plus the `(24,)` `price_table` array test; (b) **enforcement half — complete** — `test_override_any_nonoverridable_field_raises` parametrized over **all 9** non-overridable physics constants (`v_cutin_mps`, `v_rated_mps`, `v_cutout_mps`, `k_T_per_c`, `eta_inverter`, `eta_ch`, `eta_dis`, `soc_min`, `soc_max`) — each raises `DeviceModelError`; verified 9 distinct fields; (c) **cross-area invariant guarded** — `test_gansu_model_ids_match_registry_json` reads both files and asserts the 4 device-model IDs == `registry.json` keys verbatim (registry-drift guard); (d) **unit-count single-source** — `get_unit_counts()` exposes the canonical §4.1 rounding (`round(615/4.2)=146` wind, `round(294.5/300)=1` battery; explicit site `unit_count` overrides the formula) so consumers never re-derive it. **Scope v1.0.0 = the 4 Gansu device models** (the power-supply scenario, master-plan §7.1 v1); non-Gansu §8 compositions are additive future entries. **Versioning:** adding device models or new *optional* fields = **minor** (no re-LOCK; a non-Gansu composition yields a different `(obs_dim, action_dim)` and gets its own site-specific checkpoint — the LOCKED Gansu 107/6 checkpoint is NOT reopened); any field removal/rename/retype or a change to the non-overridable/overridable composition rule = **major** → superseding DECISION + re-LOCK + re-review. **Consumers:** env-build resolver (Workstream B, this PR); Workstream A (geo-UI references device-model IDs), C (multi-year sim resolves per-site `EnvParams`), D (economics block keyed by the same ID). **Producer:** jax-env-engineer. Does NOT touch the LOCKED telemetry, checkpoint, or `registry.json` contracts.
- [LOCKED] 2026-06-12 — `contracts/shared/config_validation.md` v1.0.0 (PR #89, head `c06f951`; task #66; realizes D32(i)). rl-architect LOCK on own authority (shared routing; both advisories resolved — frontend-reviewer CLEAR, backend-reviewer COMMENT-resolved). The two-tier site-config validator: **the resolver layer is the single source of truth** for all semantic/physics/econ config validation, exercised **exactly once** (no duplicate TS/serving rule sets). API: `validate(site, device_models) -> ValidationResult{errors[], warnings[]}` — **non-raising, COLLECT-ALL** (returns every failing rule, never short-circuits); `resolve_site()` calls `validate()` first and **raises `ConfigValidationError`** on non-empty errors (coexists with `DeviceModelError`, both in `resolver.py`). `ValidationIssue = {rule_id (stable, never-renamed; `E-`/`W-<CATEGORY>-<MNEMONIC>`), field, message, constraint-with-numbers}`. Tiers: **errors REJECT** the config; **warnings proceed-with-explicit-ack**. Serving exposes **`POST /api/site/validate`**; the A wizard stage-① cannot proceed on a hard error; UI renders field-level (no duplicate rules). **Joint ownership:** jax-env (physics-plausibility — C-rate vs per-unit ratio, capacity sanity, unservable-load, unit_count consistency) + finance-expert (econ — WACC domain, senseless prices, off-benchmark CAPEX/OPEX). **Future rules are GATED on their dependencies (not blocking):** `E-TAR-SHAPE` → `(12,24)` post device_model_schema v2.0.0 (PR #87); `W-WACC` on `finance.wacc_pct`; economics/electrolyzer rules on their config blocks — the contract is complete now, rules activate as deps land. **Versioning:** adding rules / activating gated rules = **minor, no re-LOCK**; removing/renaming a `rule_id` or changing the `ValidationResult`/`ValidationIssue` shape = **major → superseding DECISION + re-review**. Precedents D26 (two-tier), D18 (single-validator), D32(i). **Flagged future DECISION:** §11.2 warning-ack keying is `rule_id`-only in v1; device-lists will need a rule-level-vs-per-device-instance ack choice (frontend-reviewer flag). Contract+tests LOCKED; the resolver implementation of the rules is the next stage → QA. Consumers: serving (`validate` endpoint), the A wizard, the resolver. Producers: jax-env (resolver + physics rules) + finance-expert (econ rules).
- [LOCKED] 2026-06-12 — `contracts/shared/device_model_schema.md` **v2.0.0** (PR #87, merged `6fbcf7a`; **re-LOCK** of the v1.0.0 keystone; task #58 Step A). The seasonal `price_table` reshape `(24,)→(12,24)`: `EnvParams.price_table` is now `(12,24)` month×hour ¥/MWh (`PRICE_TABLE_SEASONAL_YPW`); the jitted step indexes `price_table[month, hour]` (`month = MONTH_OF_STEP[t]`) at the **3 lookup sites** (`obs[5]`, the forecast block, the cost stage). **Human-gate:** the USER's Option-B decision authorized reopening the LOCKED contract (CLAUDE.md human-escalation for altering a LOCKED contract — met). **Bit-parity preserved:** `cn-gansu` = the v1.0.0 24-vector replicated ×12 → `price_table[m,h] == old[h] ∀m`, full-trajectory bit-identical to the v1.0.0 baseline; verified by the 288-assertion construction-parity sweep + the **month-selection mechanism tests** (`test_month_of_step_boundaries`, `test_get_obs_price_uses_correct_month` — the parity suite is structurally blind to a month-index bug since all rows are identical, so the mechanism is tested directly) + `test_step_output_parity_fixed_seed` (un-skipped + fixed). **`obs_dim` stays 107, `action_dim` 6** (obs carries price *values*; month already in `obs[9]/[10]`) → `checkpoint_format` LOCK unaffected. Real seasonal provincial data lands as data-only (v2.0.x) via `tariff_model_schema`; a seasonal env is a *new env* → its own training run + checkpoint (the flat-Gansu checkpoint is NOT reopened). Versioning: adding optional fields = minor; further shape/format change = major → DECISION + re-LOCK. The v1.0.0/v1.1.0 composition-rule + economics terms carry forward. Producer jax-env; consumers: resolver, `tariff_model_schema` (sources the `(12,24)` table).
- [LOCKED] 2026-06-12 — `contracts/shared/tariff_model_schema.md` **v1.0.0** (PR #91, merged `e9ecfd1`; task #58 Step B; realizes the §58 tariff library, D31/F1 + D32(i)). The seasonal regional tariff library: `config/tariff_model_schema.yaml` keyed by **region ID** (`cn-gansu`, `cn-guangdong`, …). Each region carries a `(12,24)` month×hour buy-price table (¥/MWh, on `device_model_schema` v2.0.0), `demand_rate` (¥/MW·month), D7 `sell_clamp` (spread + σ), and a **display-only currency code** (the env stays ¥-pure internally). **The region ID is a binding A↔B join key:** the site YAML gains an optional `tariff_region`; the resolver sources tariff params from the matching region entry, **absent → inline fallback** (backward-compat). `cn-gansu` = the 24-vector replicated ×12 → bit-parity with the merged v2.0.0 `PRICE_TABLE_SEASONAL_YPW`. **Validation (config_validation-integrated):** `E-TARIFF-SHAPE` (shape ≠ `(12,24)` — hard error), **`E-TARIFF-DEMAND`** (`demand_rate < 0` — **HARD ERROR**, rl-architect ruling: a negative demand charge is commercially impossible, *unlike* negative spot/spread prices which genuinely occur in oversupply markets → `W-TARIFF-SPREAD-NEG` warning; the two-tier line is impossible=error, suspicious-but-legal=warning), `W-TARIFF-CURRENCY-UNKNOWN`. `validate_tariff_region() -> ValidationResult` (single-source, exactly once). Versioning: adding regions/fields = minor; removing/renaming a field or changing the `(12,24)` shape = major → DECISION. Producer jax-env; consumers: resolver (`tariff_region` join), A geo-picker, serving. Implementation merged (#91, QA 45/45).

## Open blockers

- none. [B1] resolved by [D3]–[D10] (PR #2). [B2] resolved by [D11] (PR #2): parity reference is a from-scratch NumPy implementation under `reference/`, not vendored legacy. The telemetry shared contract is now LOCKED (see above, PR #6).
